{% extends 'base.html.twig' %}
{% block title %}Hello SeanceController!{% endblock %}

{% block body %}
    <div id='showSeance'> 
        <h1>{{seance.nom}}</h1>

        <table>
            <thead>
                <tr>
                    <th>Exerice</th>
                    <th>Serie</th>
                    <th>Repetition</th>
                    <th>poid</th>
                </tr>
            </thead>

            {% for exercice in seance.getSeanceExercices %}
            </tbody>
                <tr>
                    <td><a href="{{ path('show_exercice',{'id':  exercice.getExercice.getId}) }}">{{exercice.getExercice}}</a></td>
                    <td>{{ exercice.getSerie}}</td>
                    <td>{{ exercice.getRepetition}}</td>
                    <td>{{ exercice.getPoid}}</td>
                </tr>
            </tbody>
            {% endfor %}
        </table>


        <div class='formAddSeance'>
            <form action="{{ path('add_exericeToSeance' ,{'seance_id': seance.id}) }}" method="post" > 
                <label for="exerice-select">Choisie un exerice :</label>
                <select name="exercices" id="exercice-select">
                    {% for exercice in exercices %}
                        <option value="{{ exercice.id }}">{{ exercice.nom }}</option>
                    {% endfor %}
                </select>
                <label for="serie">Serie :</label>
                <input type="number" name="serie">
                <label for="repetition">Repetition :</label>
                <input type="number" name="repetition">
                <label for="poid">Poid :</label>
                <input type="number" name="poid">
                <input type="submit" value="ajouter exercice"></i>
            </form>
        </div>
    </div>
{% endblock %}
